HIVE v1.26 R2 write, skill and model picker patch

Changes:
- Upload object keys now keep readable filenames under date-based R2 paths.
- Configured R2 lanes can be read/write when shared server-side write credentials allow it.
- Added inline object view endpoint: GET /v1/files/r2/{lane}/view?key=...
- Upload APIs accept a lane target for multipart, text and base64 uploads.
- Added POST /v1/skills/from-file to register an uploaded object as a searchable skill descriptor in D1.
- Model discovery now marks image, video, audio and infrastructure model types as selectable rather than discovery-only.
- Production readiness accepts shared write credentials as the multi-bucket read path.

Verification:
- PYTHONPATH=backend python -m pytest backend/tests -q: 154 passed
- python -m ruff check backend/app backend/tests scripts: passed
- production_preflight.py --allow-warnings: ready true, error_count 0

Note:
- python -m pip check reports unrelated sandbox-level package conflicts: gtts/click and moviepy/pillow.
